<div class="box">
  <h4>Reading <%= link_to "#{@reading.book.title}", user_book_path(@user, @book) %></h4>
  <%= render 'reading_totals' %>

  <%= link_to "Edit", edit_user_book_reading_path(@user, @book, @reading), class: "btn link-btn" %>
</div>